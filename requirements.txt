# AI Scraping Stack — Requirements
# pip install -r requirements.txt

# ── Core ────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.7.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# ── Layer 1: Scraping ──────────────────────────────────────────────
scrapling>=0.4.0

# ── Layer 2: Local LLM ─────────────────────────────────────────────
ollama>=0.3.0

# ── Layer 3: MCP ───────────────────────────────────────────────────
fastmcp>=0.9.0

# ── Layer 4: Cloud LLM ─────────────────────────────────────────────
openai>=1.40.0

# ── Dev / Testing ──────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# ── Optional providers (uncomment as needed) ───────────────────────
# crawl4ai>=0.6.0        # best open-source LLM crawler, free
# firecrawl-py>=1.7.0    # best markdown quality, $16/mo
# scrapegraphai>=1.14.0  # prompt→JSON, self-healing
# playwright>=1.45.0     # headless browser (for screenshots)
# instructor>=1.4.0      # structured LLM output
